workflow:
  rules:
    - if: '$CI_COMMIT_TAG'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: never
    - when: always

include:
  - project: 'ORG_REDACTED/template/ci-template'
    ref: 'main'
    file: '/bitstream.yml'